import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brainwareuniversity.rooms',
  appName: 'Brainware Rooms',
  webDir: 'out'
};

export default config;
